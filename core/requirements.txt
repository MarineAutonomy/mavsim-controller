# Python dependencies for mavsim example controller
#
# Install with: pip install -r requirements.txt

# REST API client
requests>=2.28.0

# ROS WebSocket client (rosbridge)
roslibpy>=1.6.0

# Optional: For more advanced controllers
numpy>=1.21.0

# Flask
flask>=2.0.0

# Sensor bridge for local sensor streaming (Task 2.5: needed when tests mount sensor_bridge)
websockets>=11.0
# mavsim-sensor-bridge (installed from ../sensor_bridge during Docker build when possible)

# Optional: For plotting results
# matplotlib>=3.5.0

# ROS2 Python packages (for recording service)
# Note: These are installed via ROS2 Humble packages in Dockerfile
# rclpy and rosbag2_py are provided by ros-humble-ros-base and ros-humble-rosbag2

